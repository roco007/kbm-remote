/**
 * @kbm-remote/ui-components
 *
 * Shared design system:
 *   theme/       — tokens for light/dark (UX Design Document §3.1)
 *   components/  — cross-platform primitives (TODO M5)
 *   hooks/       — theme + accessibility hooks (TODO M5)
 */

export * from "./theme";

export const UI_COMPONENTS_PLACEHOLDER = true;
