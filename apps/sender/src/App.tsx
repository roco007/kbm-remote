/**
 * @kbm-remote/sender entry — Expo router will be wired in M5.
 *
 * Screens (UX Design Document §4):
 *   DevicesHome (S1) → Connection (S2) → TouchpadHub (S3)
 *   Keyboard (S4), MediaControls (S5), ClipboardSync (S6)
 *   FileTransfer (S7), PresentationMode (S8), Settings (S9)
 *
 * TODO (M5): implement with expo-router.
 */

export default function App() {
  return null; // scaffold only — no business logic
}
