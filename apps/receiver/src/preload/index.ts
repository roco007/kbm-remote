/**
 * Electron preload bridge — exposes a minimal, sandbox-safe API surface
 * from the main process to the renderer (dashboard UI).
 *
 * TODO (M5): implement contextBridge channels.
 */

export const PRELOAD_PLACEHOLDER = true;
