/**
 * Renderer (React dashboard) entry point — screens R1–R12 per the
 * UX Design Document (onboarding, dashboard, pairing, trusted devices,
 * settings, permissions, logs).
 *
 * TODO (M5): implement with React + Tailwind + @kbm-remote/ui-components.
 */

export const RENDERER_PLACEHOLDER = true;
