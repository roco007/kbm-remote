/**
 * Electron main process entry point.
 *
 * Responsibilities (Architecture §6):
 *   - Bootstrap the NestJS application context in-process
 *   - Create the system tray icon and menu
 *   - Manage window lifecycle (dashboard) and OS notifications
 *
 * TODO (M0/M1): implement.
 */

async function main() {
  // Reserved for M0/M1 bootstrap.
}

void main();
